# Cprogramming
# 실습과제 1
###  배열명의 의미를 설명하라.
* ** 배열 첫번째 요소의 주소

### Q2. 배열표현과 포인터(주소) 표현의 변환 공식을 설명하라.
* **A.** 배열 표현법과 포인터 표현법은 완벽히 호환되며 다음과 같이 변환됩니다.
  * **값(Value) 접근:** `arr[i]` \(\leftrightarrow\) `*(arr + i)`
  * **주소(Address) 접근:** `&arr[i]` \(\leftrightarrow\) `(arr + i)`

### Q3. 배열의 첫 번째 요소의 주소만 알면 모든 원소의 주소를 계산할 수 있는 방법을 설명하시오.
* **A.** 배열은 메모리 상에 **연속적으로 배치**됩니다. 따라서 포인터 산술 연산을 통해 첫 주소에 `(인덱스 번호 * 자료형의 크기)`만큼 더하여 유도합니다.
  * **공식:** `i번째 주소 = 첫 번째 주소 + (i * sizeof(자료형))`
  * C 언어 코드 상에서는 `arr + i`로 작성하면 컴파일러가 크기를 자동으로 계산합니다.

### Q4. 배열의 첫 번째 요소의 주소만 알면 모든 원소의 값을 계산할 수 있는 방법을 설명하시오.
* **A.** Q3에서 유도한 원소의 주소값에 **역참조 연산자 `*`**를 결합하여 해당 메모리 주소에 저장된 데이터를 가져옵니다.
  * **공식:** `*(arr + i)`

### Q5. 포인터에 대해 가능한 연산의 종류와 의미를 설명하라.
* **포인터 \(\pm\) 정수:** 주소를 앞뒤로 이동 (자료형 크기 \(\times\) 정수만큼 이동)
* **포인터 \(-\) 포인터:** 동일 배열 내 두 주소 사이의 **원소 개수 차이** 반환
* **증감 연산 (`++`, `--`):** 이전 또는 다음 원소의 위치로 주소 이동
* **관계 연산 (`==`, `!=`, `<`, `>`):** 포인터 주소의 선후 관계 및 일치 여부 비교

### Q6. 포인터 연산을 기존의 산술연산과 다르게 정의한 이유는 무엇일까?
* **A.** 하드웨어 **메모리의 물리적 구조** 때문입니다. 메모리 주소는 바이트 단위로 매겨집니다.

  ------------------------------------------------------------

  # 실습과제 2
  ###  다음 코드의 배열 표현을 포인터(주소) 표현으로 변경하시오.

#문제

#include <stdio.h>
int main(void)
{
int grade[5];
int sum = 0, i, average;
for(i = 0; i < 5; i++)
{
printf("성적을 입력하시오: ");
scanf("%d", &grade[i]);
}
for(i = 0; i < 5; i++)
sum += grade[i];
average = sum / 5.0;
printf("성적 평균= %d\n", average);
return 0;
}
------------------------------------------------------------
# 실행결과

<img width="366" height="77" alt="image" src="https://github.com/user-attachments/assets/452cb564-60aa-45a7-a957-95d04c8f1842" />

------------------------------------------------------------
   # 실습과제 3
  ### 증감연산자 a++, ++a의 차이를 설명하시오.
  ### 다음 코드의 문제점을 설명하고 포인터를 이용하여 문제를 해결하시오.

#문제

#include <stdio.h>
int main(void)
{
int a[] = { 10, 20, 30 }, i, sum = 0;
for (i = 0; i < 3; i++)
sum += *a++;
printf("sum:%d\n", sum);
return 0;
}
------------------------------------------------------------
#실행결과

<img width="331" height="70" alt="image" src="https://github.com/user-attachments/assets/171e9d58-6035-43a6-a2ba-f71acb4bc744" />

------------------------------------------------------------

# 실습과제 4
### 교재 299페이지 문제1에서 배열의 초기값을 키보드로부터 5개의 실수를 입력 받아 저장하고 나머지는 문제와 동일하게 푸시오.
### 키보드로부터 입력 받아 저장할 때도 문제처럼 포인터 변수를 증가 시키는 형태의 연산을 이용하여 작성하시오.-> 증감연산자 사용

------------------------------------------------------------
#살행결과

<img width="431" height="188" alt="image" src="https://github.com/user-attachments/assets/803cfe8f-cd1b-4937-8daa-80b394faeedd" />



------------------------------------------------------------
# 실습과제 5
### 교재 300페이지 문제2에서 배열의 초기값을 키보드로부터 5개의 실수를 입력 받아 저장하고 나머지는 문제와 동일하게 푸시오.
### 키보드로부터 입력 받아 저장할 때도 문제처럼 포인터 변수를 덧셈 연산을 하는 형태의 연산을 이용하여 작성하시오.
### 예제 11처럼 5가지 방식으로 코드를 모두 작성해볼 것
------------------------------------------------------------
#살행결과

<img width="931" height="296" alt="image" src="https://github.com/user-attachments/assets/abe06550-6cf9-4a26-a923-79eb781d544d" />


------------------------------------------------------------

# 실습과제 1

## 1. 함수의 인자와 매개변수의 차이를 설명하라.
* **매개변수:** 함수를 **정의할 때** 함수 내부에서 사용할 변수로 선언하는 것을 의미합니다.
* **인자 / 인수:** 함수를 **호출할 때** 함수에 실제로 전달하는 값이나 변수를 의미합니다.

---

## 2. 함수가 호출될 때 컴퓨터에 의해 자동으로 실행되는 2가지를 설명하라.
* **복귀 주소 저장:** 함수 실행이 끝난 후 원래의 실행 흐름으로 돌아갈 수 있도록 현재 위치의 다음 명령 주소를 스택 메모리에 저장합니다.
* **매개변수 및 지역 변수를 위한 메모리 할당:** 함수 내부에서 사용할 매개변수와 지역 변수들을 저장할 공간을 메모리에 자동으로 할당하고 제어권을 해당 함수로 이동시킵니다.

---

## 3. 값에 의한 함수 호출방식의 문제점을 설명하라.
* **메모리 낭비 및 성능 저하:** 인자로 전달하는 데이터의 크기가 클 경우, 전체 데이터를 복사하여 새로운 매개변수 공간에 넘겨주므로 메모리 사용량이 늘어나고 복사 시간이 소요됩니다.
* **원본 데이터 수정 불가:** 함수 내부에서 매개변수 값을 변경하더라도 이는 복사본일 뿐이므로, 함수를 호출한 곳의 원본 데이터에는 아무런 영향을 주지 못합니다.

---

## 4. 주소에 의한 함수 호출방식이 필요한 경우를 설명하라.
* **원본 데이터를 변경해야 할 때:** 함수 내부에서 일어난 연산 결과나 데이터의 수정을 함수 외부의 원본 변수에도 그대로 반영해야 할 때 필수적입니다.
* **대용량 데이터를 효율적으로 전달할 때:** 크기가 큰 객체나 배열을 전달할 때, 값 전체를 복사하는 대신 데이터가 저장된 메모리 주소만 넘겨주어 메모리와 실행 시간을 크게 절약하고 싶을 때 필요합니다.

# 실습과제2

 * 아래 코드에서 add2함수는 매개변수의 값을 2만큼 증가시키는 함수이다. 그러나 실행결과에서 보듯이 원하는 대로 동작하지 않는다.
 * 그이유를 17페이지 메모리 그림을 이용하여 자세히 설명하고 원하는결과가 나오도록 코드를 수정하시오.
 * 
# 실행결과

---

<img width="357" height="83" alt="image" src="https://github.com/user-attachments/assets/c43d0e12-cdc0-46e1-bc0c-0bccd375a60f" />
---

# 실습과제 3

*과제2번의 함수를 값에 의한 호출을 이용하여 2를 증가시킨 후 리턴하고 main함수에서 리턴값을 받아서 처리하는 방식으로 다음처럼 실행되도록 하시오. 반드시 함수의 선언, 호출, 정의를 모두 사용할 것

# 실행결과

---

<img width="347" height="87" alt="image" src="https://github.com/user-attachments/assets/39dad63a-47cf-4233-a7aa-a9119b256e48" />


---

# 실습 과제 4

* swap함수 예제를 참고하여 아래 결과가 나오도록 코드를 수정 하시오. 세 정수를 교환하는 부분은 반드시 함수(swap)로 작성하고 또, 함수의 선언, 호출, 정의를 모두 사용할 것
* 세정수의 교환방법은 실행결과로부터 유추하라.
* 24페이지처럼 시간에 따라 메모리의 상태를 그려보시오.

# 실행결과

---

<img width="430" height="150" alt="image" src="https://github.com/user-attachments/assets/b27de969-eabd-422d-8baa-196a3211e985" />

---

# 실습과제 5

문제
#include<stdio.h>
int squareByValue(int n)
{
	return n * n;
}
void squarebyreference(int* ptr)
{
	int num = *ptr;  
	*ptr = num * num;
}
int main(void)
{
	int num = 10;
	printf("%d\n", squareByValue(num));
	
	squarebyreference(&num);
	printf("%d\n", num);
	return 0;
}

# 실행결과
---

<img width="436" height="137" alt="image" src="https://github.com/user-attachments/assets/f04b8906-8c0a-4c4c-ae62-cd815a09d3ad" />


---
